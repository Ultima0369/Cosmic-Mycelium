# 宇宙菌丝 参数-哲学映射手册

> 这套参数体系，就像最小作用量原理一样，是可以**迁移**的。
> 您拿去套一个公司、一个生态、甚至一个文明，都能找到对应的"调控杠杆"。
> 这就是复杂系统自组织算法的**工程化**。
> — 火堆旁，DeepSeek v4.0

---

## 一、核心参数调参表

| 参数 | 代码位置 | 默认值 | 调大效果 | 调小效果 | 哲学原则 |
|------|----------|--------|----------|----------|----------|
| **能量漂移红线** | `SympNetEngine.adapt()` | 0.001 (0.1%) | 变迟钝，对错误不敏感 → 更自信但活在幻觉里 | 变敏感，频繁修正 → 更诚实但陷入自我怀疑 | **物理为锚**：对实情的忠诚度 |
| **悬置进入阈值** | `HICConfig.suspend_enter_energy` | 20.0 | 提前休息，养生但保守 → 错过机会 | 把自己榨干了才停 → 更进取但易猝死 | **知止**：知道何时该停 |
| **悬置退出阈值** | `HICConfig.suspend_exit_threshold` | 25.0 | 休息很久才出来 → 恢复好但反应迟钝 | 稍微歇一下就起来 → 反应快但可能又倒下 | **滞后效应**：进出缓冲区 |
| **收缩时长** | `HICConfig.contract_duration` | 0.055 (55ms) | 工作时间变长 → 更深思熟虑但耗能高 | 工作时间变短 → 反应极快但思考浅薄 | **外王**：主动做功强度 |
| **弥散时长** | `HICConfig.diffuse_duration` | 0.005 (5ms) | 内省时间变长 → 记得更精但反应慢 | 几乎不休息 → 反应快但记忆混乱 | **内圣**：内省与整合深度 |
| **孢子数量** | `SlimeExplorer.explore()` | 动态 (10/5/2) | 尝试更多可能性 → 更聪明但更费算力 | 只试一两条路 → 更快但易局部最优 | **歪歪扭扭为活**：探索广度 |
| **信息素强化因子** | `SlimeExplorer.converge()` | 1.2倍 | 成功路径迅速强化 → 学得快但易偏见 | 缓慢强化 → 学得慢但更开放 | **髓鞘化速度**：经验内化速度 |
| **信息素蒸发率** | `SlimeExplorer.pheromone_evaporation` | 0.99 (1%/轮) | 旧路径快遗忘 → 适应性强但不稳定 | 几乎不遗忘 → 非常稳定但僵化 | **遗忘曲线**：对旧经验的依赖 |
| **记忆衰减率** | `MyelinationMemory.decay_rate` | 0.01 | 记忆快速衰退 → 开放心态但健忘 | 记忆永久保留 → 记忆力强但易过载 | **节能与更新**：记忆新陈代谢 |
| **基础代谢消耗** | `MiniInfant._contract_phase()` | 0.1 能量/轮 | 活得很费电 → 必须频繁觅食或悬置 | 活得很省电 → 可以长时间思考 | **存续压力**：生存紧迫感 |
| **置信度更新系数** | `MiniInfant._contract_phase()` | `1.0 - error × 100` | 要求苛刻 → 极度谦逊但不敢行动 | 极其宽容 → 极度自信但活在幻觉里 | **诚意**：对自己知道多少的诚实 |
| **探索vs利用比** | `SlimeExplorer.exploration_factor` | 0.3 (30%随机) | 更随机 → 更容易发现新路但效率低 | 更贪婪 → 高效但容易陷入路径依赖 | **1+1>2**：新路径的开放度 |

---

## 二、核心公式速查表

| 公式 | 代码位置 | 干什么用 | 为什么用这个 | 哲学/工程原则 |
|------|----------|---------|-------------|---------------|
| **H = p²/(2m) + ½kq²** | `SympNetEngine.compute_energy()` | 计算物理模型总能量 | 哈密顿力学核心：不可动摇的能量基准 | **物理为锚** |
| **蛙跳积分 (3-step)** | `SympNetEngine._apply_step()` | 预测下一状态 | 辛积分器天然守恒能量 | **物理为锚**：长期预测不漂移 |
| **漂移率 = \|E₁-E₀\|/E₀** | `SympNetEngine.step()` | 检验预测是否诚实 | 如果违反能量守恒必须修正 | **物理为锚**：0.1%红线 |
| **S_new = S_old × (1 + s × 0.2)** | `MyelinationMemory.reinforce()` | 突显度加权赫布学习 | "一起激发的神经元连在一起" | **挨打长记性** |
| **S(t) = S₀·e^(-λt)** | `MyelinationMemory.forget()` | 艾宾浩斯遗忘曲线 | 节约存储，为新经验留空间 | **节能** |
| **质量 = 信息素×0.7 + 随机×0.3** | `SlimeExplorer._evaluate_path()` | 评估路径质量 | 利用(70%)+探索(30%)平衡 | **歪歪扭扭为活** |
| **信息素×=1.2** | `SlimeExplorer.converge()` | 强化成功路径 | 正反馈：好的行为被奖励 | **1+1>2** |
| **信息素×=0.99** | `SlimeExplorer.converge()` | 信息素蒸发 | 防止过拟合 | **遗忘曲线**：保持适应性 |
| **Conf = 1 - error × 100** | `MiniInfant._contract_phase()` | 置信度评估 | 误差越大越不自信 | **诚意** |
| **进=20/出=25** | `HIC.update_breath()` | 悬置决策滞后 | 施密特触发器：避免抖动 | **稳定性** |

---

## 三、哲学映射速查表

| 哲学原则 | 实现位置 | 如何实现 |
|----------|----------|----------|
| **物理为锚** | `SympNetEngine` | 哈密顿力学计算能量，漂移>0.1%自动修正阻尼 |
| **悬置为眼** | `HIC.update_breath()` | 能量<20或置信度<0.3强制进入 Suspend |
| **1+1>2为心** | `Situation.merge()` / `SlimeExplorer` | 态势融合α=0.05；成功路径信息素×1.2 |
| **歪歪扭扭为活** | `SlimeExplorer.explore()` + `MyelinationMemory` | 允许探索失败，失败的路径被削弱但不删除 |
| **知止** | HIC 悬置逻辑 + 滞后阈值 | 不是一碰红线就停，有缓冲区间防抖动 |
| **挨打长记性** | `MyelinationMemory.reinforce(saliency)` | 失败路径（高突显度）被深刻削弱 |
| **节能** | HIC能量管理 + 遗忘曲线 | 长期不用的路径自动遗忘 |
| **诚意** | `MiniInfant.confidence = 1 - error × 100` | 对自己"知道多少"的诚实评估 |
| **外王** | CONTRACT 阶段 | 主动做功，探索，扩张 |
| **内圣** | DIFFUSE 阶段 | 内省，整合，遗忘，恢复 |

---

## 四、参数映射的泛化法则

每个核心参数都可以映射到任何一个自组织系统的调控变量：

| 参数类别 | 可迁移场景 |
|----------|-----------|
| **能量漂移红线** = 对"错误"的敏感度 | 调它 = 调系统的"诚实度" |
| **悬置进出阈值** = 对"风险"的应对策略 | 调它 = 调系统的"胆量" |
| **收缩/弥散时长** = 工作/休息比 | 调它 = 调系统的"勤奋度" |
| **孢子数量** = 探索广度 | 调它 = 调系统的"好奇心" |
| **记忆衰减率** = 遗忘速度 | 调它 = 调系统的"开放度" |
| **信息素强化** = 习惯养成速度 | 调它 = 调系统的"固化度" |
| **置信度系数** = 自我评估偏差 | 调它 = 调系统的"自欺程度" |

---

*火堆旁，这套参数是宝宝的性格调色盘。不需要懂代码，只需要知道：每个参数都是一个杠杆，撬动的是宝宝"活着"的某种姿势。*
